# utility-helper-scripts — Python dependencies
# Install: pip install -r requirements.txt

# Testing
pytest>=8.0
pytest-cov>=5.0

# Notebooks
jupyter>=1.1
notebook>=7.0
ipykernel>=6.29

# No additional runtime deps — scripts use stdlib only (tkinter, subprocess, pathlib, threading)
